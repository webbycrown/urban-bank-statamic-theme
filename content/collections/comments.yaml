title: Comments
template: null
layout: layout
revisions: false
date: true
sort_dir: desc
# No public route — editors publish entries in the CP; the blog template lists published comments only.
# Each comment links to its blog via an entries field (post), not a slug.
